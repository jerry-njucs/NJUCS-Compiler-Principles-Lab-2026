# 编译原理 实验一 词法&语法分析

## 程序实现的功能

本程序基于 Flex 和 Bison 实现了 C-- 语言的词法分析与语法分析，并能够构建抽象语法树（AST），同时具备词法/语法错误检测与细粒度错误恢复能力。

### 词法分析功能
*   **基础词法识别**：支持识别符合 C-- 语言定义和要求的各类标识符、整数、浮点数及操作符。

*   **注释处理与 EOF 检测**：正确支持 `//` 单行注释与 `/* */` 多行注释，能够报告多行注释未闭合的错误。

### 语法分析与 AST 构建
*   **基于 LALR(1) 的语法推导**：完整实现了 C-- 文法规则，正确处理算符优先级与悬挂 else 问题。
*   **语法树构造**：自己定义了Node数据结构，在规约时采取多叉树结构挂载节点，并在可能因错误恢复产生 `NULL` 节点的关键路径（如 `FunDec`、`CompSt`、`IF-ELSE` 条件分支）增加了防御性空指针判断，消除了段错误风险。

### 错误恢复功能
*   处理了缺失分号、非法声明越界混入语句块、括号不匹配、未闭合注释等典型错误，通过较为细粒度的错误恢复方法，能够在出现错误之后，继续检测后续程序中存在的错误。
*   保障了在发生局部语法错误时，外层控制流（`while`、`if-else`）及函数体的结构完整性。

---

## 编译与运行说明

我使用了框架代码中已经给出的Makefile进行编译，cd进入Code目录下，使用**make lexical**编译词法分析部分，使用**make parser**编译整个项目，使用类似于./Code/parser Test/test1.cmm这样的命令行来运行单个示例，错误或者语法树通过stdout进行输出，分析结束后程序可以正常退出，返回值为0.

## 一些实现细节

### 扩展错误拦截
按实验分组要求，在词法分析阶段主动拦截并报告特定的非法格式（作为 Type A 错误丢弃，不向语法层传递干扰Token，防止应用更加复杂的错误恢复逻辑）：

*   非法浮点数（如无整数部分的 `.85`）。
*   非法八进制（如 `015`）与非法十六进制（如 `0x33`）。
*   科学计数法浮点数（如 `0.23e5`）。

### 基于“同行抑制”的级联错误解析
有一部分词法报错出现之后，会在同一行再次触发语法报错，这与实验手册中“同一行最多有且仅有一个错误”相矛盾，因此我维护了lastErrorLine这个全局变量，抑制同行的二次报错。

在 LALR(1) 状态机中，遇到缺失类型关键字的声明（如 `Person person;`）时，解析器遇到了开头的ID之后，会被迫将状态推进至 `StmtList`，导致随后合法的局部变量声明（如 `int a;`）引爆“语句区非法声明”的误报（B8）。

由于这个错误经常会和其他错误恢复实现互相矛盾，且没有好的方法来阻止编译器提前进入StmtList,因此我采用了一种比较奇怪的解法：不修改底层文法，而是在 `Stmt` 规则的动作中精准捕获此类特征（`ID ID SEMI`），在报出首个错误后，通过 C 代码逻辑前瞻性地将 `lastErrorLine` 强制设定为**下一行的行号**。以此实现了物理层面的级联静音，以极小代价完美压制了 LALR(1) 的固有误判。

### 定点窄恢复与边界保护
为防止 `error` 符号过度吞噬 Token，设计了极其克制的窄恢复策略：
*   **控制流结构保护**：对于 `while` 和 `if`，设计了 `WHILE LP error RP` 等定点修复。当且仅当条件表达式内部出错时同步到右括号，绝不越界吞噬后续代码块（`CompSt`）。
*   **右括号缺失补偿**：针对 `while (j < (n) {` 类型的缺右括号故障，补充了 `WHILE LP error CompSt` 路线，遇到左花括号立即停止 `error` 吞噬，完美保全函数体结构。
*   **函数参数抢救**：针对函数参数列表错误导致整个函数体被跳过的致命问题，在 `FunDec` 级追加了 `ID LP error RP`，保证参数出错时也能无缝滑入函数体内部继续扫描深层错误。

### 非法越界声明的精准拦截
对于混入语句区（`StmtList`）的变量声明（如 `int n = 4;`），摒弃了带有 `error` 占位符的匹配方案。直接在 `Stmt` 中使用 `TYPE DecList SEMI` 进行生吞。这种方法不包含 `error` 关键字，不会误触 Bison 的错误释放机制，实现了精准上报错误且零副作用恢复。