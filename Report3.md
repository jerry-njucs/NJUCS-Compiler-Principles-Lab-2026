# 编译原理 实验三 中间代码生成

### 231220060 侯兴宇

## 程序编译与运行说明

使用老师提供的makefile进行编译，进入项目目录Code之后，使用`make parser`进行编译，生成解析器parser。之后使用 `./parser <path_to_testcase> <path_to_output_ir>` 即可测试对应的测试用例。

## 整体思路和结构

本部分实验在之前正确实现词法&语法&语义分析过程之后，采用语法制导翻译的思想，通过对之前语义分析时创建的AST进行深度优先遍历，实现了C--的中间代码生成。

### 数据结构

*   **Operand** 代表中间代码中的操作数，包含了变量、常量、临时变量、标签以及地址等不同类型。
*   **InterCode** 单条中间代码，不同类型的代码具有不同成员（采用 union 结构区分）。
*   **CodeList** 中间代码的链表，利于创建/插入/连接，正好对应翻译代码的顺序和深度优先遍历的顺序相同。
*   **Symbol**：在符号表的数据结构 `Symbol_` 中，特别增加了 `is_param` 成员，用来标记变量是否为函数参数，这对解决传引用时的取址问题至关重要。

### 翻译逻辑

1.  **核心翻译框架**：实现了 `translate_Exp`、`translate_Stmt`、`translate_Cond` 等系列递归翻译函数。底层语句生成的 `CodeList` 通过 `join_codelist` 不断向上层拼接，最终在根节点生成完整的程序代码。
2.  **左值地址抽象**：为了统一一维数组（`Exp[Exp]`）和结构体成员（`Exp.DOT`）的读写，专门抽象设计了 `translate_Exp_Addr` 函数。无论左值多么复杂，该函数统一求出 `基址 + 偏移量`，并返回所指向的内存地址操作数，随后通过 `READ_MEM` 或 `WRITE_MEM` 完成统一访存。
3.  **短路求值**：在条件表达式翻译时，通过继承属性 `label_true` 和 `label_false` 控制分支跳转，实现了高效的布尔表达式短路计算。

## 选做部分实现

### 与符号表的交互
为了支持数组申请和结构体读写，中间代码生成阶段必须获得跨阶段的类型信息。
程序通过 `lookup(name)` 直接查询实验二中构建的符号表，进而使用 `get_type_size()` 算出变量的总字节大小（用于生成 `DEC` 指令），使用 `get_field_offset()` 累加出结构体成员的具体偏址。
此外，针对结构体/数组传参问题，结合新加的 `is_param` 字段，程序在翻译地址时能够轻松区分：当局部变量取址时使用 `GET_ADDR` (`t := &x`)；当参数已是指针传入时直接赋基址 (`t := x`)，消除了多层指针取址的错误隐患。

### 针对选做4.2的拦截
根据实验要求，对于未支持的多维数组及数组传参情况需要给出特定提示。程序在 `get_vardec_size_and_id` 分配内存和 `translate_ParamDec` 翻译形参的地方增加了查表拦截。一旦获取到 `ARRAY` 的子元素依然是 `ARRAY`，或者参数直接判定为数组类型，则立即向 stdout 打印 `Cannot translate: Code contains variables of multi-dimensional array type or parameters of array type.` 并通过 `exit(0)` 进行正常退出，阻断非法中间代码文件的生成。

### 一个抽象的错误
在刚实现完“查符号表来计算内存大小”的逻辑时，发现查询全部失败，偏移量皆退化为0导致程序崩溃。仔细排查后发现，是 `semantic_check()` 函数在退出前就自动调用了 `destroy_symbol_table()` 销毁了符号表！
这导致后续 `generate_intercode` 阶段面对的是一张空表。通过取消提前销毁的动作，使得符号表这一核心字典贯穿了编译器的分析与生成阶段。